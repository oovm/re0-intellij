package restart.language.psi

import com.intellij.psi.tree.IElementType
import restart.RestartLanguage

class RestartTokenType(debugName: String) : IElementType(debugName, RestartLanguage) {
//    override fun toString(): String = "RestartTokenType.${super.toString()}"
}

